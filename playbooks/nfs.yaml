- hosts: nexus
  vars:
    nfs_paths:
      - /hulkpool
      - /nas
      - /var/lib/technis
  roles:
    - storage/nfs/server
  tags:
    - server

- hosts: all:!nexus
  vars:
    nfs_paths:
      - /hulkpool
      - /nas
      - /var/lib/technis
  roles:
    - storage/nfs/client
  tags:
    - client