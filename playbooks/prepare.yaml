- hosts: all
  roles:
  - role: raspi
    when: inventory_hostname in groups['arm64']
  - os/apt-cacher
  - os/package/update
  - role: os/package/upgrade
    when: upgrade | default(False) | bool
  - os/package/install
  - os/prepare
