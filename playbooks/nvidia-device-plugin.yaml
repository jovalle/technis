- hosts: control_plane[0]
  tasks:
    - name: deploy nvidia-device-plugin daemonset
      command: kubectl apply -f https://raw.githubusercontent.com/NVIDIA/k8s-device-plugin/v0.10.0/nvidia-device-plugin.yml