# =============================================================================
# CrowdSec Acquisition Configuration
# =============================================================================
# Defines log sources for CrowdSec to analyze
# =============================================================================

# -----------------------------------------------------------------------------
# Traefik Access Logs (Primary source)
# -----------------------------------------------------------------------------
filenames:
  - /var/log/traefik/access.log
labels:
  type: traefik
---
# -----------------------------------------------------------------------------
# Auth Logs (System-level attacks)
# -----------------------------------------------------------------------------
filenames:
  - /var/log/auth.log
labels:
  type: syslog
