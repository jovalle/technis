services:
  whoami:
    labels:
      - traefik.enable=true
      - traefik.http.routers.whoami.middlewares=external-bypass@file
      - traefik.http.routers.whoami.rule=Host(`whoami.techn.is`)
      - traefik.http.services.whoami.loadbalancer.server.port=80
    networks:
      - external
