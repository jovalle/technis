services:
  valkey:
    cap_add:
      - SETGID
      - SETUID
    cap_drop:
      - ALL
    command:
      - valkey-server
      - --save
      - "60"
      - "1"
      - --loglevel
      - warning
    container_name: valkey
    deploy:
      resources:
        limits:
          cpus: 1
          memory: 512M
        reservations:
          cpus: 0.1
          memory: 64M
    healthcheck:
      interval: 10s
      retries: 5
      start_period: 10s
      test:
        - CMD
        - valkey-cli
        - ping
      timeout: 5s
    hostname: valkey
    image: ghcr.io/valkey-io/valkey:9-alpine
    labels:
      - gatus.group=data
      - gatus.priority=P2
      - traefik.enable=false
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=64m
    user: 1000:1000
    volumes:
      - ${STACK_DATA_ROOT:?STACK_DATA_ROOT is required}/valkey:/data
