REPO_ROOT="$(cd "$(dirname "$DIRENV_FILE")" && pwd)"
VIRTUAL_ENV="$REPO_ROOT/.venv"
export VIRTUAL_ENV
export PATH="$VIRTUAL_ENV/bin:$PATH"

# Default Compose env file at repository scope.
export COMPOSE_ENV_FILES="$REPO_ROOT/docker/.env"
