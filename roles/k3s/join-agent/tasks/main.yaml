- name: join existing k3s cluster as an agent
  shell: >
    curl -sfL https://get.k3s.io | K3S_TOKEN={{ cluster_token | default('technis') }} K3S_URL=https://{{ k8s_server | default(groups.control_plane[0]) }}:6443 sh -
  args:
    warn: no