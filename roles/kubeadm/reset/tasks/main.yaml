- name: revert cluster deployment
  shell: kubeadm reset -f

- name: remove CNI configuration
  file:
    path: /etc/cni/net.d
    state: absent

- name: stop kubelet
  systemd:
    name: kubelet
    state: stopped
    enabled: no

- name: iptables
  command: "{{ item }}"
  with_items:
  - iptables -F
  - iptables -t nat -F
  - iptables -t mangle -F
  - iptables -X
  - iptables --flush
  ignore_errors: yes
